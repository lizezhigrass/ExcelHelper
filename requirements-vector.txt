# Excel 快捷查询助手 向量检索扩展依赖
# Vector search extension — for qdrant_vector backend
# Install AFTER core: pip install -r requirements-core.txt
#                     pip install -r requirements-vector.txt

# ── Qdrant 向量数据库客户端 ────────────────────────────────
qdrant-client>=1.7.0      # Qdrant 官方客户端（HTTP / gRPC 双模式）
grpcio>=1.50.0            # gRPC 通信（prefer_grpc=True 时使用，端口 6334）

# ── 向量计算 ───────────────────────────────────────────────
numpy>=1.24.0             # 向量数组运算

# ── HTTP 客户端（qdrant-client 的 HTTP 传输层依赖） ─────────
httpx>=0.24.0
requests>=2.28.0          # vector_api REST 后端依赖

# ── 数据验证（qdrant-client 模型层） ──────────────────────
pydantic>=2.0.0
